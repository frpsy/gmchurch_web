{
  "name": "gmchurch-web",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "bulletin:sync": "node scripts/sync-bulletins.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.0",
    "pdfkit": "^0.19.1"
  }
}
